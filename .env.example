# Database (SQLite for local dev, Postgres for production)
DATABASE_URL="file:./dev.db"
# DATABASE_URL="postgresql://user:password@localhost:5432/conference_connect"

# Auth secret (generate with: openssl rand -base64 32)
JWT_SECRET="change-me-to-a-random-secret-at-least-32-chars"

# OpenAI (optional — app falls back to keyword matching without it)
OPENAI_API_KEY=""

# File storage (local for dev)
UPLOAD_DIR="./public/uploads"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Demo mode (pre-seeds fake users for testing)
DEMO_MODE="true"
